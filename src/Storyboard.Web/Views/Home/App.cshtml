@{
    ViewBag.Title = "Storyboard";
}
@section head
{
    <base href="/Home/App">
}

<app-home>Loading...</app-home>

@section scripts
{
    @await Html.PartialAsync("_AngularScripts")
    <script>
        System.config({
            packages: {
                '../../app': {
                    defaultExtension: 'js'

                }
            }
        });
        System.import('../../app/app.bootstrapper').catch(console.log.bind(console));
    </script>

}
